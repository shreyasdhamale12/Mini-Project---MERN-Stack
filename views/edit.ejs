<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com/"></script>
</head>
</head>

<body class="text-white">
    <div class="w-full min-h-screen bg-zinc-900 p-10">
        <div class="flex justify-end w-full">
            <a class="bg-red-500 rounded-md w-20 px-2 py-3 mb-3 inline-block" href="/logout">Logout</a>
        </div>
        <h2 class="inline-block mb-3">Edit your Post</h2>
        <form action="/update/<%= post._id %>" method="post">
        <textarea placeholder="What on your mind"
            class="clock p-3 w-1/3 rounded-md resize-none border-zinc-700 border-2 bg-transparent" name="content"
            id=""><%= post.content %></textarea>
        <input class="mt-3 bg-green-500 block w-40" type="submit" value="Update post">
        </form> 
    </div>
</body>

</html>