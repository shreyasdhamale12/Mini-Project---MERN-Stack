<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com/"></script>
</head>
</head>

<body class="text-white">
    <div class="w-full min-h-screen bg-zinc-900 p-10">
        <div class="flex justify-end w-full">
            <a class="bg-red-500 rounded-md w-20 px-2 py-3 mb-3 inline-block" href="/logout">Logout</a>
        </div>
        <h3 class="text-3xl w-auto p-5">Profile Account</h3>
        <div class="flex gap-3">
        <div class="w-20 h-20 rounded-md">
            <img class="w-full h-full object-cover" src="/images/uploads/<%= user.profilepic ? user.profilepic : 'default.jpg' %>" alt="">
        </div>
        <h3 class="mt-5 text-3xl">Hello,<%= user.name%>
        </div>
        </h3>
        <h5 class="mb-5">You can create new post</h5>
        <form action="/post" method="post">
            <textarea placeholder="What on your mind"
                class="clock p-3 w-1/3 rounded-md resize-none border-zinc-700 border-2 bg-transparent" name="content"
                id=""></textarea>
            <input class="mt-3 bg-green-500 block w-40" type="submit" value="Create new post">
        </form>
        <div class="posts">
            <h3 class="text-xl text-zinc-400 mt-5">Your posts</h3>
            <div class="postcontainer mt-5">
                <% user.posts.reverse().forEach((post)=> { %>

                    <div class="post w-1/3 p-2 rounded-md mb-2 bg-zinc-700 border-[1px] border-zinc-800">
                        <h4 class="text-blue-500 mb-3">
                            <%= user.username %>
                        </h4>
                        <h4 class="tracking-tight">
                            <%= post.content %>
                        </h4>

                        <small class="inline-block mt-2">
                            <%= post.likes.length %> likes
                        </small>
                        <div class="btns gap-3">
                            <a class="text-blue-500" href="/like/<%= post._id %>">
                                <%= post.likes.indexOf(user._id)===-1 ? "Like" : "Unlike" %>
                            </a>
                            <a class="text-zinc-500" href="">Edit</a>
                        </div>
                    </div>
                    <% }) %>
            </div>
        </div>
    </div>
</body>

</html>